[pytest]
# `harness` and `conftest` are imported by bare name from the test module, so this
# directory must be on sys.path.
pythonpath = .
markers =
    integration: drives real ma-core and Control-Center binaries against Redis DB 15
testpaths = .
